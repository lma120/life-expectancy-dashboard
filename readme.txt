Install Dependencies by running:
```
pip install -r requirements.txt
```

Open the app in your IDE (I used PyCharm) and run the app, it should show:
```
Dash is running on http://127.0.0.1:8050/

 * Serving Flask app 'app'
 * Debug mode: on
```